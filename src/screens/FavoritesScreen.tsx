import React from 'react'; import {FlatList,StyleSheet,Text,View} from 'react-native'; import {useSafeAreaInsets} from 'react-native-safe-area-context'; import {useNavigation} from '@react-navigation/native'; import {NativeStackNavigationProp} from '@react-navigation/native-stack'; import {RootStackParamList} from '../navigation/types'; import {useAppDispatch,useAppSelector} from '../store/hooks'; import {selectAllBreeds} from '../store/cacheSlice'; import {toggleFavorite,persistFavorites} from '../store/appSlice'; import BreedCard from '../components/BreedCard'; import {colors} from '../theme/colors'; import {Icon} from '../components/Icon';
export default function FavoritesScreen(){const insets=useSafeAreaInsets();const nav=useNavigation<NativeStackNavigationProp<RootStackParamList>>();const dispatch=useAppDispatch();const breeds=useAppSelector(selectAllBreeds);const favorites=useAppSelector(s=>s.app.favorites);const list=breeds.filter(b=>favorites.includes(b.id));return <View style={[styles.root,{paddingTop:insets.top}]}><Text style={styles.title}>Saved breeds</Text><Text style={styles.sub}>{list.length} favorite{list.length===1?'':'s'}</Text><FlatList data={list} keyExtractor={x=>x.id} contentContainerStyle={{padding:16,paddingBottom:insets.bottom+90}} renderItem={({item})=><BreedCard breed={item} favorite onFavorite={()=>{const next=favorites.filter(x=>x!==item.id);dispatch(toggleFavorite(item.id));dispatch(persistFavorites(next) as never)}} onPress={()=>nav.navigate('BreedDetail',{id:item.id})}/>} ListEmptyComponent={<View style={styles.empty}><Icon name="heartOutline" size={52} color={colors.peach}/><Text style={styles.emptyTitle}>No favorites yet</Text><Text style={styles.emptyText}>Tap the heart on a breed to save it here.</Text></View>}/></View>}
const styles=StyleSheet.create({root:{flex:1,backgroundColor:colors.bg},title:{fontSize:28,fontWeight:'900',color:colors.text,paddingHorizontal:18,paddingTop:12},sub:{fontSize:12,color:colors.muted,paddingHorizontal:18,marginTop:2},empty:{alignItems:'center',paddingTop:90,paddingHorizontal:30},emptyTitle:{fontSize:20,fontWeight:'900',color:colors.text,marginTop:10},emptyText:{color:colors.muted,textAlign:'center',marginTop:6}});
