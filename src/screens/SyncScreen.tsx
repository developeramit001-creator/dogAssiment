import React from 'react'; import {Pressable,StyleSheet,Text,View} from 'react-native'; import {useSafeAreaInsets} from 'react-native-safe-area-context'; import {useAppSelector} from '../store/hooks'; import {syncAll} from '../store/syncService'; import {colors} from '../theme/colors'; import {Button,Card,OfflineBanner} from '../components/UI'; import {formatDate} from '../utils/format';
export default function SyncScreen(){const insets=useSafeAreaInsets();const s=useAppSelector(x=>x.sync);const refresh=()=>syncAll();return <View style={[styles.root,{paddingTop:insets.top}]}><Text style={styles.title}>Sync center</Text><OfflineBanner online={s.online}/><Card><Text style={styles.cardTitle}>Library status</Text><Text style={styles.big}>{s.cachedCount}</Text><Text style={styles.label}>breeds cached on this device</Text><View style={styles.line}><Text style={styles.muted}>Last synced</Text><Text style={styles.value}>{formatDate(s.lastSync)}</Text></View><View style={styles.line}><Text style={styles.muted}>Connection</Text><Text style={[styles.value,{color:s.online?colors.sage:colors.danger}]}>{s.online?'Online':'Offline'}</Text></View>{s.syncing&&<Text style={styles.progress}>Syncing pages… {Math.round(s.progress*100)}%</Text>}{s.error&&<Text style={styles.error}>{s.error}</Text>}<Button title={s.syncing?'Syncing…':'Sync now'} onPress={refresh}/></Card><Card><Text style={styles.cardTitle}>Offline-first behavior</Text><Text style={styles.muted}>The library is persisted locally. When the network returns, a background sync refreshes the 6 API pages and keeps cached data visible if a page fails.</Text></Card></View>}
const styles=StyleSheet.create({root:{flex:1,backgroundColor:colors.bg,paddingHorizontal:16},title:{fontSize:28,fontWeight:'900',color:colors.text,padding:18},cardTitle:{fontSize:16,fontWeight:'900',color:colors.text},big:{fontSize:42,fontWeight:'900',color:colors.coral,marginTop:12},label:{color:colors.muted},line:{flexDirection:'row',justifyContent:'space-between',marginTop:14},muted:{color:colors.muted,lineHeight:21},value:{fontWeight:'900',color:colors.text},progress:{color:colors.sage,fontWeight:'800',marginVertical:14},error:{color:colors.danger,marginVertical:14,fontSize:12,fontWeight:'700'}});
